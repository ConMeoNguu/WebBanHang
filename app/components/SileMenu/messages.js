/*
 * SileMenu Messages
 *
 * This contains all the text for the SileMenu component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SileMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SileMenu component!',
  },
});
